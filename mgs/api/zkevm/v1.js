const axios = require("axios");
const config = require("../../config/config").default;

// fetch latest prices, and set recommendations - v1
const zkevmV1FetchPrices = async (_rec) => {};

export default zkevmV1FetchPrices;
